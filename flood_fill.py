'''Code a simple flood fill program that reads a map from input.txt
    Cell types: . can be traversed, # cannot
    Outputs: The same map, but each . is substituted by a number, two cells
    have the same number if and only if they're in the same connected component
'''
